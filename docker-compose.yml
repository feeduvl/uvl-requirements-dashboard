version: "3"
services:
  javamongo:
    image: mongo:latest
    container_name: "javamongo"
    ports:
      - 27017:27017
  springboot-mongodb:
    image: springboot-mongodb:1.0
    container_name: "springboot-mongodb"
    restart: always
    ports:
      - 8080:8080
    links:
      - javamongo