server:
  ssl:
    key-alias: spring-jira
    key-store: classpath:jira.jks
    key-store-type: JKS
    key-store-password: password
  port: 9645

spring:
  data:
    mongodb:
      database: Jira-issues
      host: mongo
      port: 27017
